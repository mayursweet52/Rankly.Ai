{
  "current_architecture": "Enterprise HRMS (Phase 2 - Dynamic Employee Database)",
  "active_features": {
    "auth": "OrganizationMember junction table active. First user gets Super Admin automatically.",
    "frontend": "Multi-workspace selection portal active. Roles Builder UI (public/roles.html) active. Employee Dynamic Fields UI (/admin-fields.html) active. Employee Directory (/employees.html) active. Employee Profile Form (/employee-form.html) active.",
    "rbac": "Admin-defined dynamic roles. Data scopes supported (NO_DATA, MY_DATA, SUBORDINATES, ALL_DATA)."
  },
  "roles": {
    "Sumit": "MUST DO: Review `/employees.html`, `/employee-form.html` and `/admin-fields.html`. Ensure they are linked correctly to the main dashboard (`index.html`). Add missing animations, polish the UI, and verify Tailwind v3 compilation.",
    "Mayur": "MUST DO: Prepare for Phase 3 (Time & Attendance). Check if `/api/employees` and `/api/admin/custom-fields` are working securely via RBAC middleware.",
    "Vaibhav": "MUST DO: Phase 2 SQLite DB is pushed. Next, prepare Prisma schema for Phase 3 (Attendance & Shifts) and manage Postgres migration mapping."
  }
}
