{
  "current_architecture": "Zoho-Style Multi-Tenant (Many-to-Many)",
  "active_features": {
    "auth": "OrganizationMember junction table active. Auto-Link Email Onboarding active.",
    "frontend": "accept-invite.html active. Needs Workspace Switcher UI."
  },
  "roles": {
    "Sumit": "MUST DO: Build the 'Workspace Switcher' dropdown UI in index.html (Dashboard). MUST NOT: Touch backend APIs.",
    "Mayur": "MUST DO: Build the GET /api/auth/workspaces and POST /api/auth/switch-workspace endpoints to feed Sumit's UI. MUST NOT: Edit HTML templates.",
    "Vaibhav": "MUST DO: Ensure prisma db push ran successfully on production and Employee.userId unique constraints are resolved."
  }
}
